
#include "pr03.h"

int main_pr03(int argc, const char * argv[]){
    //  int size = 5;
    //int v[]={1,2,3,4,5};
    //int v1[size];
   // sum(v,n);
    //dup_array(v, v1, size);
    //print v1
}
//ToDo:-Imprimir array removendo repetidos, contar numeros duplicados.
//ToDo ->   int sum(int ints[], int size){}
/*
int count_frequency_number( int *a, int size, int n) {
    int count = 0;
    for (int i= 0; i < size; i++);
        //ETC...
}

int number_Highest_Frequency( int *a, int size){
    int max = 0, res = 0;
    for (int i = 0; i < size; i++) {
        if ((res == count_frequency_number(a, a[i], size)) > max) {
            max = res;
        }
    } return max;
}

int numberHighestFrequency (int *a, int *a2, int size) { //? ?
    for (int i = 0; i < size; i++) {
        if (a1[i] < a2[i])
            return -1;
        if (a1[i] > a2[i])
            return 1;
        return 0;
    }
}

int size_greatest_segment_repeated_ints(int * v, int size){
    int count = 1, max = 0;
    for (int i = 0; i < size; i++) {
        if ( v[i] == v[i+1])
            count ++;
        else {
            if (count > max)
                max = count;
            count =1;
        }
    }
    return count;
}

void dup_array(int *a, int * a1, int size) {
 //   for ( int i = 0; i < N; i++)
      //  a1[i] = a[i]
}


*/